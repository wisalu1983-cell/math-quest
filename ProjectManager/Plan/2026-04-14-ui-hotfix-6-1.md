# UI 6.1 Hotfix 执行计划

> **编写日期**：2026-04-14  
> **来源规格**：`ProjectManager/Specs/2026-04-14-ui-redesign-spec.md` § 6.1  
> **父项**：UI/UX 整体重设计（本计划是其 hotfix 子计划）  
> **状态**：✅ 代码完成；全量 lint/build 验证受仓库既有问题阻塞

---

## 一、目标

在不等待整版 UI 重构的前提下，先完成 `§ 6.1` 中定义的 8 个独立 hotfix，修复当前版本中已经明确存在的 UI / 无障碍问题，并把执行结果同步回项目长期记录。

**本次只处理：**
- C-01：`SessionSummary` 失败态背景 token 错误
- C-02：`SessionSummary` 在 render 期间调用 `setPage`
- A-C02：移除 `user-scalable=no`
- A-N03：按页面动态更新 `document.title`
- A-M04：补 `prefers-reduced-motion`
- A-S04：答题反馈 `aria-live`
- A-M03：心数展示补 `aria-label`
- N-03：移除 `Profile` 中硬编码“五年级”

**本次不处理：**
- `§ 6.2` 中已明确归入整版改造的组件级 / 结构级问题

---

## 二、涉及文件

**代码文件：**
- `src/pages/SessionSummary.tsx`
- `src/pages/Practice.tsx`
- `src/pages/CampaignMap.tsx`
- `src/pages/Profile.tsx`
- `src/App.tsx`
- `src/styles/globals.css`
- `index.html`

**测试文件：**
- `src/utils/ui-accessibility.test.ts`

**新增辅助文件：**
- `src/utils/ui-accessibility.ts`

**项目记录：**
- `ProjectManager/ISSUE_LIST.md`
- `ProjectManager/Overview.md`
- `ProjectManager/Plan/README.md`

---

## 三、执行策略

### 任务 1：建立最小可回归的 UI/a11y 辅助函数与测试

**目的：**
为本轮适合纯函数验证的行为建立低成本测试基线，避免把所有验证都压在人工检查上。

**文件：**
- Create: `src/utils/ui-accessibility.ts`
- Create: `src/utils/ui-accessibility.test.ts`

**步骤：**
1. 先写失败测试，覆盖以下纯逻辑：
   - 页面标题映射
   - 心数 `aria-label` 文案
   - 答题反馈播报文案
2. 运行对应测试，确认先红。
3. 再实现最小辅助函数，供 `App.tsx` / `Practice.tsx` / `CampaignMap.tsx` / `SessionSummary.tsx` 复用。
4. 复跑测试，确认转绿。

**命令：**
- `npx vitest run src/utils/ui-accessibility.test.ts`

---

### 任务 2：完成全局层 hotfix

**目标：**
先处理对全局影响最大的 3 项修复。

**文件：**
- Modify: `index.html`
- Modify: `src/App.tsx`
- Modify: `src/styles/globals.css`

**具体项：**
1. viewport 移除 `maximum-scale=1.0, user-scalable=no`
2. `App.tsx` 监听 `currentPage` 动态设置 `document.title`
3. `globals.css` 新增 `prefers-reduced-motion: reduce` 降级策略

**验证：**
- `npm run build`
- `npm run lint`

---

### 任务 3：完成结算页 hotfix

**文件：**
- Modify: `src/pages/SessionSummary.tsx`

**具体项：**
1. `bg-error/10` 改为 `bg-danger/10`
2. `lastSession` 为空时，改为 `useEffect` 中跳回首页
3. 为心数展示补 `aria-label`，内部 emoji 设为装饰性内容

**验证重点：**
- 无 render 期副作用
- 失败态 banner 样式生效

---

### 任务 4：完成答题页 / 地图页 / 个人页 hotfix

**文件：**
- Modify: `src/pages/Practice.tsx`
- Modify: `src/pages/CampaignMap.tsx`
- Modify: `src/pages/Profile.tsx`

**具体项：**
1. `Practice` 增加屏幕阅读器可播报的反馈区
2. `Practice` / `CampaignMap` 的心数容器补 `aria-label`
3. `Profile` 去掉硬编码“五年级”

**验证重点：**
- 正确/错误反馈能生成合理播报文案
- 心数读法为“剩余生命 X 颗，共 3 颗”

---

### 任务 5：收口验证与文档回写

**文件：**
- Modify: `ProjectManager/ISSUE_LIST.md`
- Modify: `ProjectManager/Overview.md`
- Modify: `ProjectManager/Plan/README.md`
- Update this file status to complete if all验证通过

**步骤：**
1. 运行针对性测试
2. 运行 `npm run lint`
3. 运行 `npm run build`
4. 用 `ReadLints` 检查本轮改动文件诊断
5. 回写项目长期记录，标记本轮 6.1 hotfix 已完成或注明阻塞

---

## 四、验收标准

- `§ 6.1` 的 8 个 hotfix 均已落地
- 新增纯函数测试通过
- `npm run lint` 通过
- `npm run build` 通过
- 本轮执行结果已同步到 `Overview` / `ISSUE_LIST` / `Plan/README`

---

## 五、风险与说明

- 当前项目缺少现成的 React 组件测试基础设施；本轮优先为可抽离的纯逻辑补自动化测试，其余 DOM 语义改动以静态检查 + 构建验证 + 代码审查收口。
- `§ 6.1` 是改版前 hotfix，不主动扩大到 `§ 6.2` 的结构性重构，避免范围蔓延。

---

## 六、执行结果（2026-04-14）

**已完成：**
- `§ 6.1` 规定的 8 项 hotfix 均已落地
- 新增 `src/utils/ui-accessibility.ts` 与对应测试，覆盖标题映射、心数文案、答题反馈播报文案
- 已同步回写 `ISSUE_LIST.md`、`Overview.md`、`Plan/README.md`

**验证结果：**
- `npx vitest run src/utils/ui-accessibility.test.ts` ✅ 通过（3/3）
- `ReadLints` 针对本轮改动文件检查 ✅ 无新增诊断
- `npm run lint` ⛔ 未通过，阻塞项主要来自仓库既有问题：`VerticalCalcBoard.tsx`、生成器测试文件、`Practice.tsx` 既有 hooks/any 问题等
- `npm run build` ⛔ 未通过，阻塞项为仓库既有未使用符号：`mental-arithmetic.ts`、`multi-step.ts`、`qa-v3.test.ts`

**结论：**
- 本轮 hotfix 代码已完成并写回项目记录
- 仓库级全绿验证尚未达成，原因已记录为既有问题，不归因于本轮 6.1 修改
