# v0.5 Phase 3 · ISSUE-067 结构化错因反馈

> 创建：2026-04-29
> 所属版本：v0.5
> 父计划：[`../phases/phase-3.md`](../phases/phase-3.md)
> 设计规格：[`../01-research-catalog.md`](../01-research-catalog.md)、[`../../../ISSUE_LIST.md`](../../../ISSUE_LIST.md)
> 功能 current spec：[`../../../Specs/a03-vertical-calc/current.md`](../../../Specs/a03-vertical-calc/current.md)
> Spec impact：update-at-phase-close（已回写 A03 current spec）
> 状态：✅ 完成（实现、QA、current spec 回写与 `ISSUE-067` 关闭归档已完成）

---

## 一、背景

`ISSUE-067` 的现象是：多行乘法竖式中，学生最终积填对，但部分积、合计过程格或小数乘法训练格填错时，系统会判错；统一失败面板只显示正确答案和解析，不说明“为什么答案对还没通过”。这会让用户误以为判题 bug。

v0.5 预研已确认：判定规则不改，过程 / 训练格错误仍不通过；要修的是结构化错因传递和展示。Phase 3 要把这个链路做成可复用能力，Phase 4 长除法严格过程判定才能直接沿用。

### 工作脉络

1. v0.4 legacy 单行竖式已具备 `failureReason='vertical-process'`，但只覆盖进位 / 退位格。
2. v0.4 多行乘法竖式落地时，`MultiplicationVerticalBoard` 只返回 `boolean`，没有把部分积、合计行或小数训练格错误传给 Practice。
3. v0.5 Phase 1 已确认后续题型板不得继续使用裸 `boolean` 作为提交结果，必须返回带错因的 payload。
4. Phase 3 先补多行乘法结构化错因；Phase 4 长除法再复用同一套失败明细、反馈面板和错题本展示能力。
5. 2026-04-29 产品 / 技术评审补充：失败面板文案需更贴近五年级学生表达；乘法错误分类先抽成纯函数并用红灯测试覆盖；错题本只展示不重做的行动路径缺口转入 `BL-013` 候选，不进入 Phase 3。

### 前置相关规格（开工前必读）

| 规格 / 计划 | 本计划继承的硬约束 |
|---|---|
| [`../01-research-catalog.md`](../01-research-catalog.md) | 过程格错误按类别显示，不暴露具体值；训练格错误显示具体类型、用户值、正确值；错题本显示，历史记录 UI 不显示。 |
| [`../02-classification.md`](../02-classification.md) | `ISSUE-067` 是 `BL-010` 的硬依赖，属于多行乘法竖式、统一结果 UI、错题数据。 |
| [`../04-execution-discipline.md`](../04-execution-discipline.md) | 严格区分过程格和训练格；底层可保留 `failureReason`，用于未来分析。 |
| [`2026-04-28-v05-phase1-开工对齐与跨phase边界.md`](./2026-04-28-v05-phase1-开工对齐与跨phase边界.md) | 多行乘法板回调从 `boolean` 升级为结构化 payload；错因类型归属 `src/types/index.ts`。 |
| [`../../../ISSUE_LIST.md`](../../../ISSUE_LIST.md) | `ISSUE-067` 复现样例、用户影响、非目标和验证要求。 |
| [`../../../Specs/a03-vertical-calc/current.md`](../../../Specs/a03-vertical-calc/current.md) | 旧 `failureReason='vertical-process'` 已用于 legacy 单行竖式，必须兼容。 |

### Current spec 影响评估

| 项 | 结论 |
|---|---|
| 是否改变长期功能行为 / 数据契约 / QA 口径 | 是。失败反馈、错题本展示和 QA 验收口径会改变；新增字段应为可选，不要求存档版本升级。 |
| 预计回写位置 | `ProjectManager/Specs/a03-vertical-calc/current.md` |
| 待 phase 验收后回写的要点 | 多行乘法结构化错因 payload、面向五年级学生的失败面板文案、训练格错因展示、错题本展示、历史记录 UI 边界、旧错题兼容、测试 / QA 证据。 |

### 跨系统维度清单

- [x] 难度档位 / 题型梯度数：不改题目难度。
- [ ] 星级 / 进阶 / 段位数值：不改数值，但 campaign / advance / rank-match 中的错题记录都要保留错因。
- [ ] 关卡结构 / campaign.ts：不改关卡。
- [x] UI 组件 / 卡片尺寸：失败面板和错题本在 375px 竖屏下文案不溢出。
- [x] 答题形式 / 验证逻辑：不改判定；只改提交 payload、反馈展示和错题数据。
- [x] 存档 / 同步 / 数据迁移：新增可选明细字段，不 bump 版本；同步合并保留字段。
- [x] QA / 验收产物：覆盖整数多行乘法和小数乘法训练格两类用例。

## 二、问题定义

### 现状

- `src/components/MultiplicationVerticalBoard.tsx` 的 `onComplete` 只返回 `boolean`。
- `src/pages/Practice.tsx` 收到 `false` 后提交 `"竖式计算有误"`，没有传入 `failureReason` 或明细。
- 统一失败面板目前只识别 legacy 单行竖式的 `lastFailureReason === 'vertical-process'`。
- `WrongBook.tsx` 也只展示旧的“进位/退位格填写错误”。

### 用户影响

- 学生看到“正确答案是 4730”，但自己最终积也是 4730，会认为系统错判。
- 家长或测试者无法从错题本知道问题出在过程格还是小数训练格。
- Phase 4 长除法若继续使用严格过程判定，缺少错因会放大挫败感。

## 三、产品与交互口径

### 3.1 判定不变

- 最终答案错误：未通过，按普通错答展示正确答案和解析。
- 最终答案正确，但多行乘法过程格错误：未通过，展示过程类别文案。
- 最终答案正确，但小数乘法训练格错误：未通过，展示具体训练格错误。
- 最终答案正确，过程格和训练格都正确：通过。

### 3.2 展示优先级

| 场景 | 统一失败面板展示 |
|---|---|
| 最终答案错误 | 现有正确答案 + 解析；不额外暴露过程格正确值。 |
| 最终答案正确 + 只有过程格错 | “你的最终答案是对的，但竖式里的计算步骤有错误。把步骤也写对，才能通过哦。” |
| 最终答案正确 + 只有训练格错 | 展示每个训练格的类型、用户值、正确值。 |
| 最终答案正确 + 过程格和训练格都错 | 先展示过程类别，再展示训练格明细。 |

### 3.3 过程格分类

首版不逐格暴露过程格的用户值 / 正确值，只显示类别：

| code | 文案 |
|---|---|
| `multiplication-partial-product` | 部分积填写错误 |
| `multiplication-total` | 竖式求和过程填写错误 |
| `multiplication-process` | 竖式过程填写错误 |

若实现时难以低风险区分 partial 与 total，可以首版统一返回 `multiplication-process`，但必须保留扩展空间。

### 3.4 训练格分类

训练格展示具体对比：

| code | 来源字段 | 文案 |
|---|---|---|
| `decimal-operand-a-places` | `operand-a-decimal-places` | 被乘数小数位数错误 |
| `decimal-operand-b-places` | `operand-b-decimal-places` | 乘数小数位数错误 |
| `decimal-move` | `decimal-move` | 小数点移动位数错误 |

展示格式：

```text
小数点移动位数错误：你填 1，正确是 2
```

用户值为空时显示“未填写”；不显示 `undefined`、空字符串或技术 key。

### 3.5 错题本与历史记录

- 当前反馈面板：显示本次具体错因。
- 错题本：显示错因摘要和训练格明细。
- 历史记录 UI：不展示错因。
- 底层 `QuestionAttempt` / `WrongQuestion` 可以保留可选明细字段，用于未来分析。
- Phase 3 不新增错题本重做、同类题练习或跳转相关关卡入口；该行动路径缺口登记为 `BL-013` 后续候选。

## 四、工程实现方案

### 4.1 类型方案

保留旧字段，新增可选结构化明细：

```ts
export type PracticeFailureReason =
  | 'wrong-answer'
  | 'vertical-process'
  | 'vertical-multiplication-process'
  | 'vertical-training-field';

export interface PracticeFailureDetail {
  reason: PracticeFailureReason;
  source: 'legacy-vertical' | 'vertical-multiplication' | 'long-division';
  message: string;
  processCategories?: Array<{
    code: string;
    label: string;
  }>;
  trainingFieldMistakes?: TrainingFieldMistake[];
}
```

实现时可按代码风格调整命名，但必须满足：

- `failureReason` 仍是简单字符串，兼容旧 wrongQuestions / history。
- `failureDetail` 为可选对象，不触发存档版本 bump。
- 没有 `failureDetail` 的旧数据仍能按旧文案展示。

### 4.2 提交 payload

`MultiplicationVerticalBoard` 不再只返回 `boolean`，而是返回结构化 payload。建议抽出通用 payload，让后续长除法复用：

```ts
export type PracticeBoardCompletePayload =
  | { result: 'pass'; answer: string }
  | { result: 'failWrongAnswer'; answer: string; failureReason: 'wrong-answer' }
  | {
      result: 'failProcess';
      answer: string;
      failureReason: PracticeFailureReason;
      failureDetail: PracticeFailureDetail;
    };
```

legacy `VerticalCalcBoard` 可以继续映射为旧 `vertical-process`，但 `Practice` 的处理逻辑应统一消费 payload。

### 4.3 错误分析落点

| 文件 | 改动 |
|---|---|
| `src/components/MultiplicationVerticalBoard.tsx` | 使用纯函数分析最终答案、过程格、训练格错误，组件只负责调用并返回 payload。 |
| `src/pages/Practice.tsx` | `handleVerticalComplete` 消费 `failureDetail`；失败面板使用统一渲染 helper。 |
| `src/store/index.ts` | `SubmitAnswerOptions`、`QuestionAttempt`、`WrongQuestion` 支持可选 `failureDetail`；`lastFailureDetail` 支持当前反馈。 |
| `src/pages/WrongBook.tsx` | 复用错因渲染 helper；旧 `vertical-process` 数据仍显示旧文案。 |
| `src/sync/merge.test.ts` | 增加 merge 保留 `failureDetail` 的测试。 |

### 4.4 渲染 helper

建议新增纯 helper：

- `src/utils/practiceFailureDisplay.ts`
- `src/utils/practiceFailureDisplay.test.ts`

职责：

- 输入 `failureReason` + 可选 `failureDetail`。
- 输出标题、说明、过程类别列表、训练格明细。
- 处理旧数据 fallback。
- 统一 Practice 反馈面板与 WrongBook 文案。

### 4.4.1 乘法错误分类纯函数

在改造 `MultiplicationVerticalBoard` 前，先新增纯函数并用红灯测试锁定分类语义：

```ts
classifyMultiplicationErrors(
  orderedInputKeys,
  expectedByKey,
  userValues,
  finalAnswerKey,
) => PracticeFailureDetail
```

实现时可按代码风格调整参数形状，但必须满足：

- `finalAnswerKey` 错时返回普通 `wrong-answer` 路径，不额外暴露过程格错因。
- 只有部分积 / 合计行错时返回过程类别，首版可统一为 `multiplication-process`，能低风险区分时再拆 `multiplication-partial-product` / `multiplication-total`。
- 只有小数训练格错时返回 `trainingFieldMistakes`，展示训练格类型、用户值、正确值。
- 过程格和训练格都错时同时返回过程类别与训练格明细。
- 函数不依赖 React state、DOM、focus 或组件生命周期，便于长除法后续复用同类分类思路。

### 4.5 不采用的方案

| 方案 | 放弃原因 |
|---|---|
| 只把 `false` 改成 `failureReason='vertical-process'` | 能显示旧文案，但无法区分多行乘法、训练格，也不能服务长除法。 |
| 过程格逐格显示用户值和正确值 | 与预研口径冲突，会把过程答案直接暴露给学生，降低训练价值。 |
| 新增必填字段并 bump 存档版本 | 本问题不需要破坏兼容；可选字段足够。 |
| 只在当前反馈面板显示，不进错题本 | 与预研口径冲突；错题本需要作为未来分析数据来源。 |

### 4.6 实现架构决策门

| 维度 | 结论 |
|---|---|
| 模块 ownership 与依赖方向 | `MultiplicationVerticalBoard` owns 本板错误分析入口；`classifyMultiplicationErrors` owns 纯分类逻辑；`Practice` owns 统一反馈面板；`store/index.ts` owns session attempt、pending wrongQuestions 和当前反馈状态；`WrongBook` owns历史错题展示。 |
| 类型与 API 契约 | 保留旧 `failureReason` 字符串，新增可选 `failureDetail`；`MultiplicationVerticalBoard` 返回结构化 payload，不再只返回 `boolean`。 |
| 持久化 / 存档 / 版本迁移 | `failureDetail` 只作为可选字段进入 attempt / wrongQuestion；旧数据可读，无存档版本 bump；同步合并保留该字段。 |
| 标识符命名空间与向后兼容 | 新 reason 使用 `vertical-*` 命名空间；旧 `wrong-answer`、`vertical-process` 保留；训练格 code 不暴露组件内部 key 给用户。 |
| UI 容量与响应式约束 | 失败面板和错题本在 375px 竖屏下不能溢出；过程类别和训练格明细分块展示，避免一段长文挤压继续按钮。 |
| 测试与 QA 映射 | display helper 单测、乘法错误分类纯函数单测、store 单测、sync merge 测试、整数乘法和小数乘法浏览器验收共同覆盖。 |

## 五、实施拆解

| Task | 内容 | 状态 | 证据 |
|---|---|---|---|
| T1 | 写 `practiceFailureDisplay` 红灯测试：旧 `vertical-process`、乘法过程错、训练格错、空用户值 | ✅ | `src/utils/practiceFailureDisplay.test.ts` |
| T2 | 扩展 `src/types/index.ts`：新增可选 `failureDetail` 相关类型，兼容旧 `failureReason` | ✅ | `src/types/index.ts` |
| T3 | 扩展 `src/store/index.ts` 和 store 单测：`lastFailureDetail`、pending wrong questions、session attempt 均保留明细 | ✅ | `src/store/index.test.ts` |
| T4a | 新增 `classifyMultiplicationErrors` 纯函数和红灯测试，覆盖只有过程格错、只有训练格错、两者都错、最终答案错 | ✅ | `src/engine/verticalMultiplicationErrors.test.ts` |
| T4b | 改造 `MultiplicationVerticalBoard`：调用纯函数返回结构化 payload，区分最终答案错、过程格错、训练格错 | ✅ | `src/components/MultiplicationVerticalBoard.tsx` |
| T5 | 改造 `Practice.tsx` 失败面板：最终答案正确但过程 / 训练格错时显示明确原因 | ✅ | `QA/e2e/phase3-decimal-training-failure.spec.ts` 与 QA 截图 |
| T6 | 改造 `WrongBook.tsx`：显示结构化错因；旧数据 fallback 正常 | ✅ | `src/pages/WrongBook.tsx`、helper 单测 |
| T7 | 补同步合并测试，确认 `failureDetail` 不丢失 | ✅ | `src/sync/merge.test.ts` |
| T8 | 跑全量 test/build 和移动端反馈面板 QA | ✅ | `npm test -- --run`、`npm run build`、QA 产物 |

## 六、验收

- [x] 整数多行乘法：构造 `110 × 43 = 4730` 或等价多行乘法，最终积正确、部分积错误，提交后未通过，并显示“你的最终答案是对的，但竖式里的计算步骤有错误。把步骤也写对，才能通过哦。”方向文案。
- [x] 小数乘法：最终答数正确、至少一个小数训练格错误，提交后未通过，并展示训练格用户值 / 正确值。
- [x] 最终答案错误时仍按普通错答展示，不暴露过程格正确值。
- [x] 错题本展示结构化错因；历史记录 UI 不展示错因。
- [x] 旧 `failureReason='vertical-process'` 错题仍显示旧文案。
- [x] 旧 wrongQuestion 只有 `failureReason='vertical-process'`、没有 `failureDetail` 时，错题本正常 fallback，不白屏、不显示技术字段。
- [x] `failureDetail` 为可选字段，不要求存档版本升级。
- [x] 同步合并保留 wrongQuestions 中的 `failureDetail`。
- [x] 375px 手机竖屏下失败原因文案不溢出、不遮挡“继续”按钮。
- [x] `npm test -- --run` 通过。
- [x] `npm run build` 通过。

## 七、QA 用例种子

| ID | 场景 | 预期 |
|---|---|---|
| I067-01 | 整数多行乘法，部分积错、最终积对 | 判错；失败面板显示过程类别，不显示具体过程正确值。 |
| I067-02 | 整数多行乘法，最终积错 | 判错；普通正确答案 + 解析，不额外显示过程错因。 |
| I067-03 | 小数乘法，最终答数对，小数位数训练格错 | 判错；显示训练格类型、用户值、正确值。 |
| I067-04 | 小数乘法，训练格未填但最终答数对 | 判错；训练格用户值显示“未填写”。 |
| I067-05 | 错题本查看 I067-01 / I067-03 | 错因摘要可见；文案不溢出。 |
| I067-06 | 历史记录查看同一 session | 不显示错因 UI。 |
| I067-07 | 旧错题数据只有 `failureReason='vertical-process'`，没有 `failureDetail` | 错题本正常展示旧文案，不白屏、不显示技术字段。 |

## 八、回写

- Phase 3 收口已回写 `ProjectManager/Specs/a03-vertical-calc/current.md`：
  - 多行乘法竖式结构化失败 payload。
  - 面向五年级学生的过程格 / 训练格展示规则。
  - 错题本显示、历史记录 UI 不显示。
  - 可选字段不 bump 存档版本。
- 错题本重做 / 同类题行动路径不在 Phase 3 回写 current spec；该能力候选见 `BL-013`。
- `ISSUE-067` 已在 Phase 3 收口时按项目 issue 流程关闭，归档见 `ProjectManager/Plan/v0.5/issues-closed.md`。

## 九、收尾结果

- 实现入口：`src/engine/verticalMultiplicationErrors.ts`、`src/utils/practiceFailureDisplay.ts`、`src/components/MultiplicationVerticalBoard.tsx`、`src/pages/Practice.tsx`、`src/pages/WrongBook.tsx`、`src/store/index.ts`、`src/types/index.ts`、`src/sync/merge.test.ts`。
- QA 入口：`QA/runs/2026-04-29-v05-phase3-input-feedback-qa/qa-summary.md`。
- 自动化结果：`npm test -- --run` 为 59 files / 734 tests passed；`npm run build` 通过；`npx playwright test` 为 13 tests passed。
- 关闭结果：`ISSUE-067` 从 `ProjectManager/ISSUE_LIST.md` 开放清单移出，并进入 `ProjectManager/Plan/v0.5/issues-closed.md`。
