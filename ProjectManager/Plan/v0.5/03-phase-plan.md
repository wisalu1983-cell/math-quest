# 阶段规划总图

> 所属版本：v0.5
> 创建：2026-04-28
> 所属主线：[README](./README.md)
> 分类基础：[`02-classification.md`](./02-classification.md)
> 当前状态：版本包已启动，等待详细设计文档讨论

---

## 五个 Phase 一览

| Phase | 名称 | 主产出 | 启动条件 | 收尾条件 | 状态 |
|---|---|---|---|---|---|
| Phase 1 | 详细设计与架构启动门 | v0.5 详细设计文档；共享架构门结论；子计划拆分 | 版本包已建立；预研结论已汇总 | 用户确认详细设计；模块 ownership、API、存档、UI 容量、QA 映射无阻塞 | 📋 待启动 |
| Phase 2 | 竖式题样本质量诊断 | `BL-009` 抽样诊断；强制过滤规则决策；生成器 / lane 落点 | Phase 1 确认诊断口径 | 抽样结果有证据；过滤规则经用户确认；自动化样本验证通过 | 📋 待设计 |
| Phase 3 | 输入与反馈基础设施 | `BL-011` 内置键盘核心能力；系统键盘兜底入口；`ISSUE-067` 错因反馈链路 | Phase 1 确认输入与错因数据结构；Phase 2 给出竖式输入样本约束 | Chrome Android / Safari iOS 默认内置键盘通过；失败面板错因可解释；test/build 通过 | 📋 待设计 |
| Phase 4 | 竖式除法 UI 化答题 | 动态长除法核心板；小数 / 取近似 / 循环小数训练格；严格判定 | Phase 1 确认长除法数据结构；Phase 3 输入能力可复用 | 覆盖上海五年级相关题型；移动端适配通过；错因反馈接入；test/build/QA 通过 | 📋 待设计 |
| Phase 5 | Release Gate 与 Living Spec 回写 | 全版本回归；current spec 回写；Backlog / Issue 收口 | Phase 2~4 完成 | QA PASS；pm-sync-check PASS；版本包收口；发布准备完成 | 📋 待设计 |

## 建议时序

```text
Phase 1：先完成详细设计与共享架构门
  ↓
Phase 2：先做 BL-009 口算样例过滤诊断与规则
  ↓
Phase 3：再做 BL-011 内置键盘基建 + ISSUE-067 错因反馈
  ↓
Phase 4：最后做 BL-010 竖式除法 UI
  ↓
Phase 5：统一回归、规格回写、版本收口
```

Phase 2（`BL-009` 口算样例过滤）排在前面。原因是竖式除法 UI 上线前，应先确保竖式题样本与“必须通过竖式训练完成”的学习目标匹配。

Phase 3（`BL-011` 内置键盘 + `ISSUE-067` 错因反馈）排在 Phase 4 前面。原因是除法长除法 UI 的主要交互负担来自多格输入和严格错因反馈，先做共享输入 / 反馈能力能降低后续重复实现风险。

## 决策门

| 决策 | 影响文件 | 未决时处理 |
|---|---|---|
| 内置键盘是否能稳定覆盖 Chrome / Safari | Phase 1 详细设计、Phase 3 子计划 | 不能只依赖 `virtualKeyboardPolicy`；必须采用自绘输入格 + 内置键盘状态写入，并设计系统键盘独立兜底入口。 |
| 口算题过滤规则 | Phase 2 子计划、可能影响生成器 / campaign | 先抽样诊断，再决定强制规则；不在诊断前直接固化阈值。 |
| 长除法 UI 数据结构 | Phase 1 详细设计、Phase 4 子计划 | 未确认前不得进入实现；必须能表达轮次、商位、乘积、余数、落位 / 补 0、训练格。 |
| 循环小数输入与屏幕适配 | Phase 4 子计划 | 使用结构化“非循环部分 + 循环节”；循环节和答案长度设上限，超限题进入取近似题池。 |
| 错因数据记录范围 | Phase 3 / Phase 4 子计划、错题本 | 历史记录 UI 不展示；错题本展示；底层保留 `failureReason`。 |
| `BL-012` 版本归属 | Backlog / v0.6 版本包 | 不进入 v0.5；v0.6 启动时再展开后台查询权限、只读保护和生产入口。 |

## 子计划说明

具体子项进入实施前，按 [`../templates/plan-template.md`](../templates/plan-template.md) 在 `subplans/` 下创建独立子计划。当前尚未创建 `subplans/`，下一步先讨论详细设计文档，再决定 `BL-009`、`BL-011` / `ISSUE-067`、`BL-010` 的子计划拆分。
