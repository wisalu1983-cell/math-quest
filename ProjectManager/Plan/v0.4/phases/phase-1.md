# Phase 1：渲染与判定修复

> 所属：v0.4-1
> 状态：📋 待开工
> 来源：`BL-005.1` / `BL-005.3` / `BL-005.5` / `ISSUE-059`

---

## 目标

先处理低风险、用户可立即感知的竖式体验问题：看得清、判得准、交互一致，同时清理 `dec-div` 高档隐藏 `trainingFields` 的实现一致性问题。

## 范围

| 子项 | 内容 | 备注 |
|---|---|---|
| 1.1 | 竖式颜色可读性：提升数字 / 符号对比度 | 遵守阳光版 v5 UI 约束 |
| 1.2 | 小数答案兼容：`56` 与 `56.0` 等价 | 只处理数学等价输入，不放宽错误答案 |
| 1.3 | 乘法竖式交互统一 | 已确认三步路线：多位整数乘法模块 → 小数乘法复用 → 小数点定位 / 答案等价 |
| 1.4 | `ISSUE-059`：清除 `dec-div` 高档隐藏 `trainingFields` | bug 类条目，在 `ISSUE_LIST.md` 跟踪 |

## 进入条件

- 读 A03 相关规格和现有竖式代码
- 为 1.3 创建独立子计划，明确：
  1. 多位整数乘法竖式模块的交互 / 数据结构 / 验收
  2. 小数乘法如何复用整数乘法模块
  3. 小数点定位训练与 `56` / `56.0` 等价判定如何收口
- 若 1.3 子计划未落盘，可先推进 1.1、1.2、1.4

## 收尾条件

- 相关单元测试 / 组件测试覆盖判定逻辑
- 至少一轮手工验证覆盖竖式颜色和小数答案
- 乘法竖式路线有子计划和用户审核记录
- `ISSUE-059` 有关闭证据
- `npm test -- --run` 和 `npm run build` 通过
