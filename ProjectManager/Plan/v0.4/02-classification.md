# 分类与依赖关系

> 所属版本：v0.4
> 所属主线：[README](./README.md)
> 来源目录：[`01-research-catalog.md`](./01-research-catalog.md)
> 最近调整：2026-04-25 新增“题型信息架构”分类，A04/A06 断联并入 A07

---

## 五类工作

### A. 竖式笔算体验

| 子项 | 范围 | 依赖 |
|---|---|---|
| A1 | 竖式数字 / 符号颜色对比度提升 | UI token / 组件样式 |
| A2 | 小数答案兼容 `56` / `56.0` 等等价写法 | 答案判定逻辑 |
| A3 | 乘法竖式交互形式统一 | 已确认三步路线：多位整数乘法模块 → 小数乘法复用 → 小数点定位 / 答案等价 |
| A4 | 竖式难度梯度排查：进阶 3 星心算级题目、闯关第三/四关乘法桥接 | 生成器规格 + 星级梯度约束 + 关卡分布 |
| A5 | 进位格判定逻辑按难度分层 | 产品规则确认 + 反馈 UI |

### B. 题型信息架构

| 子项 | 范围 | 依赖 |
|---|---|---|
| B1 | 玩家主界面取消 A04「运算律」和 A06「括号变换」独立大题型 | Home / Campaign / Advance / Profile / Rank |
| B2 | A04/A06 断联并入，相关能力迁入 A07「简便计算」低档知识点 lane，同时保留原 A07 低档基础应用 lane | `campaign.ts` / `multi-step` generator |
| B3 | 旧 A04/A06 存档、错题、历史、段位/进阶兼容 | Repository migration / rank-match / sync |
| B4 | 运算律 lane UX 收口 | `BL-006` 槽位误导 + 操作说明 |

### C. 题目质量与内容设计

| 子项 | 范围 | 依赖 |
|---|---|---|
| C1 | 选项题扩充到 3~4 个选项，并引入概念混淆型干扰项 | 题型设计 + 生成器 / 题库逻辑；A04/A06 相关部分随 B 类先处理 |
| C2 | 闯关 / 进阶 / 段位赛重复题目诊断 | 抽题流程与 session 去重逻辑；需等 B 类 IA 稳定 |
| C3 | `dec-div` 高档隐藏 `trainingFields` 清理 | `ISSUE-059` + A03 规格 |

### D. 交互与教学引导

| 子项 | 范围 | 依赖 |
|---|---|---|
| D1 | compare tip 可达性补证 | QA 抽样策略 |
| D2 | 进位格三档反馈与通过判定 | 产品规则确认 + 反馈 UI |

### E. 工程质量

| 子项 | 范围 | 依赖 |
|---|---|---|
| E1 | Practice 答题页状态重置统一机制 | 回归测试 + 行为等价验证 |

## 依赖关系

```text
Phase 1：低风险修复
  ├─ A1 颜色
  ├─ A2 小数答案兼容
  ├─ A3 乘法竖式统一（多位整数模块优先）
  └─ C3 ISSUE-059

Phase 2：题型信息架构
  ├─ B1 玩家入口取消 A04/A06
  ├─ B2 A04/A06 断联并入，能力并入 A07 知识点 lane
  ├─ B3 存档 / 错题 / 进阶 / 段位兼容
  └─ B4 运算律 lane UX 收口

Phase 3：题目质量
  ├─ A4 竖式难度（含第四关少量 2位数×2位数桥接）
  ├─ C1 其他题型选项干扰项
  └─ C2 重复题目诊断

Phase 4：交互设计
  ├─ A5 / D2 进位格规则
  └─ D1 compare tip 补证

Phase 5：工程质量
  └─ E1 Practice 状态重置
```

## 关键边界

1. **A04/A06 取消的是题型身份，不是能力训练**：原子题型能力迁入 A07，作为 A07 自有低档知识点 lane。
2. **原 A07 低档应用不能丢**：`bracket-normal` / `extract-factor` 是从前置知识走向简便计算应用的第一步，应继续留在低档。
3. **先稳定 IA，再做重复题诊断**：`BL-008` 的样本和题型范围依赖 Phase 2 后的新题型结构。
4. **A04/A06 相关 UX 随迁移收口**：`BL-006` 不再单独作为后续 Phase 的独立 A04 UX 工作。
5. **选项题不是简单加数量**：扩到 3~4 个选项时，要加入概念混淆项，避免只增加无效干扰。
6. **进位格规则不是纯技术修复**：它改变“过程正确性”和“最终答案正确性”的关系，必须先确认教学目标。
7. **Practice 重构必须行为等价**：Phase 5 只清理状态机制，不顺手改题型逻辑或页面流程。

## 非目标归类

| 不做什么 | 原因 |
|---|---|
| 让 A07 特例调用 `operation-laws` / `bracket-ops` topic generator | A07 应直接拥有被合并能力；旧 topic id 只做 legacy 数据兼容 |
| 清空或丢弃旧 A04/A06 存档 | 违反项目级存档迁移原则 |
| 段位赛晋级动画 | `BL-002` 无新用户反馈触发，延期到 v0.5 或后续版本 |
| 大范围视觉改版 | v0.4 聚焦题目体验，UI 改动继承既有阳光版 v5 |
| 新账号 / 同步产品能力 | v0.3 已收口；v0.4 仅在新增持久化字段时遵守同步约束 |
| 重写生成器体系 | Phase 2 做 IA 聚合与兼容，不重启 v0.2 级别生成器大改 |
